# metasocli 项目约束

## 项目身份与边界

- 本项目根为 `E:\metasocli`，远端仅为 `https://github.com/3593297903/metasocli.git`。
- 用户明确要求独立新项目，不能影响现有 LibTV 项目。
- `E:\libcli` 及其源码快照、Git、业务数据、安装包和配置只读。不得在旧目录构建、安装、格式化、提交、切换分支或写入测试产物。
- 旧全局 `story2libtv` 命令、旧四份 Skill、旧 MCP 注册、旧运行目录和 LibTV 登录状态不得被覆盖或卸载。
- 不从旧项目目录动态 import，不通过旧 CLI 完成新业务，不使用 npm link、file: 依赖、符号链接、目录联接或硬链接共享旧代码或数据。复制后的源码必须在本项目独立构建。
- 分支使用 `codex/` 前缀。未经用户要求，不自动推送、发布或修改 GitHub 仓库设置。

## 复用来源

实际安装基线是 story-to-libtv 0.2.22，对应源码：

`E:\libcli\.story2libtv-work\video-prompt-entry-0.2.22-20260916\source`

`E:\libcli` 根清单是 0.2.5 且存在用户已有修改，不得视为当前正式版本。0.2.23 为候选，不能不经审查整体迁入。

只复制实施需要的源码、模板和测试，记录文件来源。不要整体复制旧仓库、`.git`、node_modules、业务数据、outputs、历史交付目录或凭据。源代码可能仍耦合 LibTV，需读依赖后迁入。

## 新命名空间

所有新身份以这里为准，覆盖早期方案中的暂定名称：

- npm 包名、CLI：`metasocli`
- 故事清单：`metasocli.yaml`
- 故事状态目录：`.metasocli`
- 用户运行目录：`%USERPROFILE%\.metasocli-runtime`
- 安装 owner：`metasocli`
- 安装回执：`.metasocli-install.json`
- 可选 MCP 注册前缀：`metasocli-`
- Skill：`metasocli`、`metasocli-video-prompts`、`metasocli-prompt-engine`、`metasocli-reference-images`；用户新增的独立旁白入口为 `metasocli-video-prompts-旁白`，原入口不自动启用旁白音频。
- 独立凭据变量：`METASO_API_KEY`

开发期使用本地构建入口。安装器如需存在，先在临时目录测试归属、覆盖、升级和卸载边界。初始化必须拒绝旧故事根和旧源码根，验证路径真实位置与目录联接。导入旧内容是明确的单向复制，不在原地迁移。

## 业务和架构

- 保留剧本与成品视频提示词两个入口、完整台词和原文追溯、素材配方、有序引用与文件哈希。
- 新建 Metaso 请求计划与视频任务模块。删除新运行链中的 LibTV 登录、CLI、画布、节点、边和远端绑定依赖。
- 采用 Node.js / TypeScript / Zod 的本地模块化程序，先保持必要依赖与锁文件稳定。第一版不引入 New API、业务数据库或常驻 Web 服务。
- 旧 contracts、project、state、service 中含有 LibTV 结构，不能原样当作通用 Core。
- H3 专属参数与提示词引用单独适配；不能沿用 Seedance 480p 或 `{{Node nodeKey}}`。
- 参考图片通过宿主原生生图或已有图片导入准备。旧 LibTV 图片补图 helper 不得进入新运行链；缺图需明确报告和恢复。
- 准备与计划命令不发起付费生成。用户明确要求的生成可在其授权范围内执行；当前初始化交接不包含付费调用授权。

## 任务正确性

- 参数和外部响应通过明确契约校验，原始提示词不被静默截断或改写。
- 提交前持久化请求意图，取得任务 ID 后尽早持久化回执。
- 创建结果未知不得盲目重新提交；本地请求哈希不等于供应商幂等保证。
- 查询与下载可以有界恢复；下载失败不能再次生成。
- 进程中断后使用原任务 ID 恢复。未知状态不能伪装为普通排队或成功。
- 使用锁与请求记录防止同一项目重复提交，输出经验证后原子保存。
- 日志不输出 API Key、认证头或完整签名链接。

## 验收与交付

- 先完成离线契约、恢复和隔离测试，再进行得到授权的真实接口验收。
- 故障测试应覆盖回执丢失、中断恢复、限流、素材变化、并发和下载失败。
- 新项目在没有可用 LibTV CLI 的环境中仍需通过自身离线测试和构建。
- 明确区分已实现、离线验证、真实验证与待办，不把测试通过写成第三方服务零故障。
- README 维护实际命令，不把拟定接口描述成已实现。
- 修改只发生在本项目及测试临时目录；结束时检查旧项目原有 Git 状态和全局命令未被改变。
